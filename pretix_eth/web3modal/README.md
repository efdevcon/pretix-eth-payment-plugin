# How to make changes to the web3modal integration:
1) setup: npm install
2) npm run dev

There is no javascript build step for this plugin - so make sure to push the generated files after any changes (web3modal-dist folder).
